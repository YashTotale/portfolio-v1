export const display = {
  isDarkMode: false,
  isNavBtnsMenuOpen: true,
  shade: "200",
  colors: {
    primary: "#fdd835",
    secondary: "#000000",
  },
};

const sampleState = { display };

export default sampleState;
